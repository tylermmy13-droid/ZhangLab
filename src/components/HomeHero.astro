<!-- ===== 首页首屏 ===== -->
<section class="home-hero">
  <div class="home-hero-content">
    <h1 class="home-hero-title" data-en="Cardiovascular research,&lt;br /&gt;grounded in clinical questions." data-zh="心血管研究，&lt;br /&gt;从临床问题出发。">Cardiovascular research,<br />grounded in clinical questions.</h1>
    <p class="home-hero-summary" data-en="Zhang Lab studies vascular injury and repair, myocardial remodeling, multi-organ interactions, wearable monitoring, and data science for cardiovascular medicine. We connect clinical observations, experimental models, and reproducible data analysis to address questions in prevention, diagnosis, and treatment." data-zh="张力课题组围绕血管损伤与修复、心肌重构、多器官互作、可穿戴监测及心血管医学数据科学开展研究。我们结合临床观察、实验模型与可重复的数据分析，探讨疾病预防、诊断与治疗中的问题。">Zhang Lab studies vascular injury and repair, myocardial remodeling, multi-organ interactions, wearable monitoring, and data science for cardiovascular medicine. We connect clinical observations, experimental models, and reproducible data analysis to address questions in prevention, diagnosis, and treatment.</p>
  </div>
  <div class="home-hero-scroll" data-home-scroll onclick="window.scrollTo({top: window.innerHeight, behavior: &quot;smooth&quot;})">
    <span data-en="Scroll to explore" data-zh="滚动探索">Scroll to explore</span>
    <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M12 5v14M5 12l7 7 7-7" />
    </svg>
  </div>
</section>

<style>
  .home-hero {
    position: relative;
    min-height: 100vh;
    min-height: 100dvh;
    overflow: hidden;
    background: var(--cream);
  }

  .home-hero-content {
    position: relative;
    top: -64px;
    display: grid;
    grid-template-columns: minmax(0, 1.28fr) minmax(280px, 0.72fr);
    align-items: center;
    gap: clamp(48px, 9vw, 160px);
    width: min(calc(100% - 96px), 1520px);
    min-height: 100vh;
    min-height: 100dvh;
    margin: 0 auto;
    padding: clamp(132px, 16vh, 180px) 0 clamp(156px, 19vh, 208px);
    animation: homeHeroFadeIn 0.9s ease-out both;
  }

  .home-hero-title {
    max-width: 950px;
    margin: 0;
    color: var(--ink);
    font-family: var(--font-title);
    font-size: clamp(48px, 6.2vw, 104px);
    font-weight: 400;
    line-height: 1;
    letter-spacing: -0.04em;
    text-align: left;
  }

  :global(html[lang="zh-CN"]) .home-hero-title {
    max-width: 980px;
    font-size: clamp(54px, 6.45vw, 106px);
    line-height: 1.08;
    letter-spacing: -0.025em;
  }

  .home-hero-summary {
    max-width: 510px;
    margin: 0 0 8px;
    color: var(--ink);
    font-family: var(--font-body);
    font-size: clamp(18px, 1.55vw, 25px);
    font-weight: 400;
    line-height: 1.55;
    letter-spacing: -0.01em;
    text-align: left;
  }

  .home-hero-scroll {
    position: absolute;
    left: 50%;
    bottom: 32px;
    transform: translateX(-50%);
    display: inline-flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
    color: var(--ink);
    font-family: var(--font-body);
    font-size: 24px;
    font-weight: 500;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    cursor: pointer;
  }

  .home-hero-scroll span {
    display: block;
  }

  .home-hero-scroll svg {
    width: 40px;
    height: 40px;
  }

  .home-hero-scroll:hover {
    color: var(--ink);
  }

  @keyframes homeHeroFadeIn {
    from { opacity: 0; transform: translateY(18px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (max-width: 900px) {
    .home-hero-content {
      top: 0;
      grid-template-columns: 1fr;
      align-content: end;
      gap: 36px;
      width: min(calc(100% - 56px), 760px);
      padding-top: 150px;
      padding-bottom: 112px;
    }

    .home-hero-title {
      max-width: 760px;
      font-size: clamp(44px, 8vw, 72px);
    }

    :global(html[lang="zh-CN"]) .home-hero-title {
      font-size: clamp(48px, 9vw, 78px);
      line-height: 1.12;
    }

    .home-hero-summary {
      max-width: 600px;
      font-size: clamp(18px, 2.8vw, 23px);
    }
  }

  @media (max-width: 640px) {
    .home-hero-content {
      width: calc(100% - 40px);
      min-height: 100svh;
      padding-top: 126px;
      padding-bottom: 96px;
    }

    .home-hero-title {
      font-size: clamp(38px, 10.5vw, 56px);
      line-height: 1.02;
      letter-spacing: -0.04em;
    }

    .home-hero-summary {
      font-size: 18px;
      line-height: 1.5;
    }

    .home-hero-scroll {
      bottom: 22px;
      font-size: 15px;
    }

    .home-hero-scroll svg {
      width: 30px;
      height: 30px;
    }
  }
</style>
